# Contributing

1. Install Pi and clone the repository.
2. Run the extension directly with `pi -e ./index.ts`.
3. Type-check with TypeScript in strict mode.
4. Keep root-agent prompt injection minimal and user-facing completion messages compact.
5. Do not weaken permission forwarding or expose child sessions in Pi's normal resume directory.
